name: newflow

on:
  workflow_dispatch:

jobs:
  newClone:
    runs-on: ubuntu-latest

    steps:
      - name: Clone Repo
        uses: GuillaumeFalourd/clone-github-repo-action@v2.3
        with:
          depth: 1
          branch: 'main'
          owner: 'VISHWATEJA1978'
          repository: 'Expense-Sharing'
